# DreezHub Guidelines

- Always validate syntax and integrity of Lua scripts upon modification.
- Always stage, commit, and auto-push changes to the GitHub repository `fraudesnaoseinaosei-max/j4rty678` (`git add .`, `git commit`, `git push origin main`).
- Always provide the anti-cache loadstring (`?v=" .. tick()`) for Roblox execution.
